name: screen_brightness
description: A Plugin for controlling screen brightness with application life cycle set and reset brightness implemented
version: 0.1.0
repository: https://github.com/aaassseee/screen_brightness/screen_brightness
issue_tracker: https://github.com/aaassseee/screen_brightness/issues

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
#  screen_brightness_platform_interface:
#    path: ../screen_brightness_platform_interface/
  screen_brightness_platform_interface: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4